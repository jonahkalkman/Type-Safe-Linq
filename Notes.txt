VRAGEN AAN DOCENT:
  Fout in returntype van include:
  Hoe kunnen we de keys van een array van de array afhalen en deze array   met keys daarna  doorsturen? (Op dit moment kunnen we de hele array doorsturen   en de keys van de array afhalen, maar niet in combinatie met elkaar) ->     BELANGRIJK!!


Vragen voor docent SWE
-  moeten er meerdere objecten zijn waarop tegelijkertijd gequeried kan worden - ja, op jonahs manier

-  Moeten de keys van een object die arrays zijn, waar op gequeried is, meegegeven worden aan het resultObject - ja. Ook mogen alleen de keys van de arrays goedgekeurd worden als deze geselect zijn (zie comment)

Voor orderBy - array.sort gebruiken (volgens sibren)


if(selection != undefined)
    selection.
     selection.Grades[0].CourseId ok
     selection.Grades[0].Grade ok